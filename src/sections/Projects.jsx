import React from "react";
import { project } from "../data/projectData";
import { Link } from "react-router-dom";

const Projects = () => {
  const lastProject = project[project.length - 1]; // get last project

  return (
    <section>
      
    </section>
  );
};

export default Projects;
